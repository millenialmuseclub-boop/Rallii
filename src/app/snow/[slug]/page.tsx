import Link from "next/link";
import { notFound } from "next/navigation";
import { OutdoorTripPlanning } from "@/components/outdoor-trip-planning";
import { findSnow, snowDestinations } from "@/snow/data";
import { SaveSnow } from "@/snow/save-snow";
export function generateStaticParams(){return snowDestinations.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const place=findSnow((await params).slug);return {title:place?.name??"Snow destination not found",description:place?.summary}}
export default async function SnowDetail({params}:{params:Promise<{slug:string}>}){const place=findSnow((await params).slug);if(!place)notFound();return <main className="rallii-editorial snow-editorial"><nav className="rallii-container trail-breadcrumb" aria-label="Breadcrumb"><Link href="/">Rallii</Link><span>/</span><Link href="/snow/">Snow</Link><span>/</span><span>{place.name}</span></nav><div className="rallii-container"><header className="trail-detail-title"><div><p className="rallii-kicker">{place.location} / {place.kind}</p><h1>{place.name}</h1></div><SaveSnow slug={place.slug} name={place.name}/></header><dl className="trail-facts"><div><dt>Season</dt><dd>{place.season}</dd></div><div><dt>Place</dt><dd>{place.location}</dd></div><div><dt>Type</dt><dd>{place.kind}</dd></div></dl><div className="trail-detail-columns"><section><p className="rallii-kicker">Why go</p><h2>A winter trip with a mountain at its center.</h2><p>{place.summary}</p><h3>Terrain character</h3><p>{place.terrain}</p></section><aside className="trail-guide"><p className="rallii-kicker">Before you go</p><h2>Check the mountain.</h2><p>Snow, lift access, avalanche exposure and open terrain change daily. Use the destination’s current report and local safety guidance.</p><a className="rallii-button" href={place.sourceUrl} target="_blank" rel="noreferrer">Official snow guidance ↗</a></aside></div><OutdoorTripPlanning place={place.location}/></div></main>}
