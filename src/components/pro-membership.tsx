"use client";
import Link from "next/link";
import { useEntitlements } from "@/hooks/use-entitlements";
export function ProMembership() {
  const pro = useEntitlements();
  return <div className="family-membership"><p className="eyebrow">{pro.isPro ? "Your membership" : "Rallii Pro"}</p><h2>{pro.isPro ? "Every way you Rallii, unlocked." : "One membership. Every way you Rallii."}</h2><p>One Pro membership covers Rail, Green, Trail, MTB and future supported Rallii experiences.</p><div className="family-benefits"><article><p className="eyebrow">Plan together</p><h3>One trip. More possibilities.</h3><p>Build collections with rail journeys, golf courses, hikes and MTB rides together. Keep private trip notes beside your plans.</p></article><article><p className="eyebrow">Rail</p><h3>Keep the journeys coming.</h3><p>Save unlimited rail journeys and use foreground Scenic Alerts on supported routes in Ride Mode.</p></article><article><p className="eyebrow">Green</p><h3>Give your next round a place.</h3><p>Organize saved courses into trip collections with notes, alongside the rail journeys that take you there.</p></article><article><p className="eyebrow">Trail</p><h3>Make room for the outdoors.</h3><p>Add hikes to shared trip collections. Enhanced trail maps, offline guides and GPS progress are planned; these Trail tools are not available yet.</p></article><article><p className="eyebrow">MTB</p><h3>Plan beyond the next descent.</h3><p>Add MTB destinations to shared collections and trip notes. Offline MTB maps, elevation profiles and ride tracking are planned, not included yet.</p></article></div><p>Discovery, course comparisons, Green saves and MTB saves remain available to everyone.</p>{!pro.purchasesAvailable ? <p className="family-membership-notice">Membership purchases are not available in this version yet.</p> : null}<div className="family-actions">{pro.purchasesAvailable && !pro.isPro ? <button type="button" disabled={pro.isLoadingEntitlements} onClick={() => void pro.purchase()}>View membership options</button> : null}<button type="button" disabled={pro.isLoadingEntitlements} onClick={() => void pro.restorePurchases()}>Restore purchases</button>{pro.purchasesAvailable ? <button type="button" disabled={pro.isLoadingEntitlements} onClick={() => void pro.refreshEntitlements()}>Refresh membership</button> : null}<Link href="/my-rallii/">Open My Rallii →</Link></div><p role="status" aria-live="polite">{pro.isLoadingEntitlements ? "Checking your membership…" : pro.error || pro.message}</p></div>;
}
