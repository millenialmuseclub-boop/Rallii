import Link from "next/link";
import { RouteMedia } from "@/components/route-media";
import { getCollectionRoutes, journeyCollections } from "@/data/journey-collections";
import type { RailRoute } from "@/types/route";

export function JourneyCollections({ routes }: { routes: RailRoute[] }) { return <section className="section-space border-t border-stone-300 pt-14 sm:pt-20" aria-labelledby="collections-title"><p className="eyebrow">Explore by experience</p><h2 id="collections-title" className="mt-2 font-serif text-4xl sm:text-5xl">What kind of journey?</h2><div className="mt-9 grid gap-5 lg:grid-cols-2">{journeyCollections.map((collection) => { const members = getCollectionRoutes(collection, routes); return <article className="collection-card" key={collection.slug}><div><h3 className="font-serif text-3xl">{collection.title}</h3><p className="mt-3 text-sm leading-6 text-stone-600">{collection.description}</p><p className="mt-5 text-xs font-bold uppercase tracking-wider text-stone-500">{members.map((route) => route.summary.name).join(" · ")}</p><Link className="primary-link mt-5" href={`/discover/${collection.slug}`}>Explore collection <span aria-hidden="true">→</span></Link></div><div className="collection-media" aria-hidden="true">{members.slice(0, 2).map((route) => <RouteMedia key={route.summary.slug} summary={route.summary} variant="card" />)}</div></article>; })}</div></section>; }
