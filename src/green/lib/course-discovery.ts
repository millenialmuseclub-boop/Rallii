import type { AccessType, Course, Experience, Setting } from "@/green/types/course";
export interface CourseFilters { query?: string; setting?: Setting | "all"; access?: AccessType | "all"; experience?: Experience | "all"; destination?: string | "all" }
export function normalizeSearch(value:string){return value.normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-zA-Z0-9]+/g," ").trim().toLowerCase()}
export function filterCourses(courses:Course[],filters:CourseFilters){const query=normalizeSearch(filters.query??"");return courses.filter(course=>{const haystack=normalizeSearch([course.name,course.shortName,course.location.city,course.location.region,course.location.state,course.location.country,course.architect,...course.character.settings,...(course.experiences??[]),...course.collections,...course.bestFor].filter(Boolean).join(" "));return(!query||haystack.includes(query))&&(!filters.setting||filters.setting==="all"||course.character.settings.includes(filters.setting))&&(!filters.access||filters.access==="all"||course.access.type===filters.access)&&(!filters.experience||filters.experience==="all"||course.experiences?.includes(filters.experience))&&(!filters.destination||filters.destination==="all"||course.destinationSlug===filters.destination)})}
export function courseOfTheDay(courses:Course[],isoDate:string){if(!courses.length)return undefined;const [year,month,day]=isoDate.split("-").map(Number);const seed=Math.floor(Date.UTC(year,month-1,day)/86400000);return courses[((seed%courses.length)+courses.length)%courses.length]}
